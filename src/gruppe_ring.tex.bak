\section{Gruppen, Ringe und Körper}
\subsection{Gruppe}
Ein Paar $(M,\circ)$ ( M ist eine Menge und $\circ$ eine zweistellige Verknüpfung), das folgende Eigenschaften besitzt:

\begin{enumerate}[$\bullet$]
\item Abgeschlossenheit bzgl. Verknüpfung $\circ$ (die Anwendung der Verknüpfung hat ein Ergebnis aus der selben Menge)
\item Assoziativgesetz: $a\circ ( b \circ c) = (a \circ b) \circ c$
\item Neutrales Element $e$: Es gibt ein Element $e$, genannt neutrales Element, sodass $e \circ a = a \circ e = a$ für alle $a$.
\item inverses Element: Zu jedem $a$ gibt es ein $b$ mit $a \circ b = b \circ a = e$, $b$ heißt das zu $a$ inverse Element.
\end{enumerate}

\paragraph*{Beispiele:} $( \mathbb{Q}, + )$, $( \mathbb{R} , + )$, $( \mathbb{Q}\setminus \{0\}, \cdot )$, $( \mathbb{R}\setminus \{0\}, \cdot )$
\\[1em]

Eine Gruppe $( G,\circ )$ heißt \textit{abelsch} oder \textit{kommutativ}, wenn $\forall a,b \in G$ die Kommutativität gilt, ansonsten gilt sie als \textit{nicht-abelsch} bzw. \textit{nicht-kommutativ}.

\begin{enumerate}[$\bullet$]
\item $a \circ b = b \circ a$
\end{enumerate}

\paragraph*{Beispiele:} $(\mathbb{Z}, +)$
\\[1em]

Eine Gruppe heißt \textit{Halbgruppe}, wenn nur die Abgeschlossenheit und die Assoziativität erfüllt sein müssen.

\paragraph*{Beispiele:} $(\mathbb{N}_0 , +)$, $(\mathbb{N}, +)$, $(\mathbb{N}_0, \cdot )$, $(\mathbb{N}, \cdot)$

\subsection{Ring}
Ein Ring ist eine Menge $M$ von ELementen zusammen mit zwei Verknüpfungen $\circ$ und $\square$, für die gelten:

\begin{enumerate}[$\bullet$]
\item $(M, \circ)$ ist eine \textit{kommutative} Guppe
\item $(M, \square)$ ist \textit{abgeschlossen} und \textit{assoziativ} (Halbgruppe)
\item Distributivgesetze: 
    \begin{enumerate}[$ $]
    \item $a \circ ( b \square c) = (a \circ b) \square (a \circ c)$
    \item $(a \circ  b) \square c) = (a \square c) \circ (b \circ c)$
    \end{enumerate}
\end{enumerate}

In einem kommutativen Ring gilt außerdem das Kommutativgesetz: $a \circ b = b \circ a$

\paragraph*{Beispiele: } $( \mathbb{Z} , + , \cdot )$, $(\mathbb{Q}, + , \circ)$

\subsection{Körper}
Ein Körper ist eine Menge $M$ von Elementen zusammen mit zwei Verknüpfungen $circ$ und $\square$, für die gelten:
\begin{enumerate}[$\bullet$]
\item $(M, \circ)$ ist eine kommutative Gruppe
\item $(M\setminus e_0 , \square)$ ist eine Gruppe ($e_0$ ist das \textit{neutrale Element} bzgl. $\circ$).
\item
\end{enumerate}